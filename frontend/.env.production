# In Vite, variables must start with VITE_ to be exposed to the client-side code.
# Put your CloudPanel Python site API URL here (no trailing slash).

VITE_API_URL=https://api-exams.astutebusinessprojects.cloud
